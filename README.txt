NAME
	late - Live Appending Text Editor

DESCRIPTION
	`late` is a simple, extensable text stream viewer and editor. It will print the live appends
	to a file similar to `tail -f` and allow you to `type stuff [enter]' and append to the file.
		This program is designed to be used with 

SYNOPSIS
	late [read file] [write file]
